const std = @import("std");
const common = @import("common");

pub fn main() void {
    std.debug.print("ghostgate (relay) running!\n", .{});
    // Example usage of shared code:
    // common.some_shared_function();
}
